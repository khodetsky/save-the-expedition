export const getUserId = state => state.user.id;

export const getUserName = state => state.user.username;

export const getUserPoints = state => state.user.points;

export const getUserCountGuessedWords = state => state.user.countGuessedWords;



