import styled from "styled-components";

export const LettersList = styled.ul`
    position: reletive;
    z-index: 1;

    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;

    padding: 50px 215px;
`