import styled from "styled-components";

export const LettersList = styled.ul`
    position: relative;
    z-index: 1;

    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 20px;

    padding: 20px 215px;
`